<%= render partial: 'shared/nav' %>
<div id="page-wrapper">
  <br>
  <div class="row">
    <% @teachers.each do |teacher| %>
    <div class="col-md-6 current-pairing-row" style="padding: 2px;">
      <div class="pane">
          <table class="table table-bordered table-hover table-striped">

          <thead>
            <tr>
              <td colspan="6">Teacher: <%= teacher.name %> | Rate: <%= teacher.find_rate %>$ an hour
              </td>
            </tr>
              <tr>
                  <th>Student Name</th>
                  <th>Email</th>
                  <th>Grade</th>
                  <th>Edit</th>
                  <th>Delete</th>
                  <th>Status</th>
              </tr>
          </thead>
          <% teacher.users.each do |student|  %>
          <tbody>
              <tr>
                  <td><%= student.name %></td>
                  <td><%= student.email %></td>
                  <td><%= student.grade %></td>
                  <td>Edit</td>
                  <td>Delete</td>
                  <td>
                    <% if student.pair_status(teacher) == "active" %>
                    <input type="checkbox" data-on="Active" data-off="Not Ready" checked data-toggle="toggle" data-size="mini">
                    <% else %>
                    <input type="checkbox" data-on="Not Active" data-off="Not Ready" checked data-toggle="toggle" data-size="mini">
                    <% end %>
                  </td>
              </tr>
              <% end %>
          </tbody>
      </table>
    </div>

    </div>
    <% end %>
  </div>

</div>
